# 형상/시험 변수 기반 고압차단기 성능 예측 분석 보고서

## 1. 요약

본 분석은 **형상 및 시험 변수를 활용하여 고압차단기의 특정 성능(분류)을 예측**하는 모델의 성능을 평가한 결과입니다. 현재 선택된 모델(`Gradient Boosting Classifier`)의 검증 지표는 정확도(Accuracy)가 0.5667로, 분류 성능이 다소 낮은 수준에 머물고 있습니다.

다만, 다양한 후보 모델 비교 분석 결과, **Light Gradient Boosting Machine (LGBM)** 모델이 F1 점수(0.6817)와 AUC 값(0.7243) 등 주요 지표에서 현 선택 모델 대비 우수한 성능을 보였습니다. 따라서 최종 운영 모델 선정 시에는 현재 선택된 모델과 LGBM의 성능 비교를 면밀히 검토하고, 해석성 및 추론 안정성을 종합적으로 고려해야 합니다.

## 2. 데이터 및 검증 조건

*   **분석 대상:** 형상/시험 변수 기반 고압차단기 성능 예측 (Classification)
*   **데이터셋 규모:** 총 293개의 행(Row)을 기준으로 분석이 진행되었습니다.
*   **특징 변수 수:** 모델 학습에 사용된 특징 변수는 총 56개입니다.
*   **타겟 변수:** `label` (분류 대상 성능 지표)
*   **검증 조건:** 전체 데이터 중 10%가 검증 세트로 할당되었으며, 이는 30개의 행(Row)을 기준으로 평가되었습니다.
*   **설계 모델명 참고:** 본 분석에 사용된 설계 모델명은 별도로 지정되지 않았습니다.

## 3. 모델 성능 평가 (선택 모델: Gradient Boosting Classifier)

현재 선택된 모델인 `Gradient Boosting Classifier`의 검증 결과는 다음과 같습니다.

| 지표 | 값 | 해석 의견 |
| :--- | :--- | :--- |
| **Accuracy** | 0.5667 | 전체 분류 정확도는 약 56.7% 수준으로, 성능 개선이 필요합니다. |
| **AUC** | 0.64 | AUC 값이 0.70 미만인 경우 모델의 결정 임계값(decision threshold) 조정이나 특징 변수 개선을 통해 성능 향상을 모색해야 합니다. |
| **F1 Score** | 0.5517 | F1 점수가 낮게 나타나, 분류 성능이 전반적으로 취약한 것으로 판단됩니다. |

**혼동 행렬(Confusion Matrix) 해석:**
전체 검증 데이터 30개 중, 실제 클래스 0은 예측 0에 9건, 예측 1에 6건으로 분류되었으며, 실제 클래스 1은 예측 0에 7건, 예측 1에 8건으로 분류되었습니다. 이는 두 클래스 간의 혼동이 발생하고 있음을 보여줍니다.

## 4. 후보 모델 비교 (Candidate Comparison)

다양한 머신러닝 알고리즘을 적용하여 성능을 비교한 결과는 다음과 같습니다.

| Model | Accuracy | AUC | F1 Score | MCC |
| :--- | :--- | :--- | :--- | :--- |
| **Light Gradient Boosting Machine** | 0.6883 | 0.7243 | **0.6817** | 0.3923 |
| Gradient Boosting Classifier (선택) | 0.5667 | 0.64 | 0.5517 | 0.1336 |
| Random Forest Classifier | 0.7 | 0.7556 | 0.6897 | N/A |

**해석 의견:**

*   **성능 우위 모델:** 후보 모델 중 **Light Gradient Boosting Machine (LGBM)**이 F1 점수(0.6817)와 AUC 값(0.7243) 측면에서 현재 선택된 모델 대비 현저히 높은 성능을 보여줍니다.
*   **모델 비교의 중요성:** Random Forest Classifier 등 다른 후보 모델들도 우수한 지표를

## 5. 검증 시각화 해석
- PDF와 채팅 미리보기의 검증 시각화 항목에서 그림별 캡션과 해석을 제공합니다.

## 6. 결론
### 종합 판단
- `Gradient Boosting Classifier`은 현재 저장된 검증 데이터 기준으로 선택된 모델이며, F1/AUC/Accuracy 기준 주요 성능은 0.5517입니다.
- 현재 세션에는 예측용 파일 결과가 없어 저장 모델의 검증 성능과 후보 모델 비교 결과를 중심으로 결론을 작성했습니다.

### 성능 및 모델 비교
- 후보 모델 비교 관점에서는 F1는 높을수록 유리한 지표이며, 후보 중 `Light Gradient Boosting Machine`의 값이 0.6817로 가장 우수합니다.
- 현재 선택 모델은 `Gradient Boosting Classifier`이며, 후보 비교 결과와 저장 모델 검증 지표를 함께 기준으로 판단해야 합니다.
- 주요 진단 포인트는 검증 세트는 30개 row 기준으로 평가되었습니다.

### 예측용 파일 반영
- 별도 예측용 파일 결과가 없으므로, 실제 배치 예측 분포나 row별 오차 평가는 포함하지 않았습니다.
- 다음 분석 자료에서는 예측용 Excel 결과를 함께 넣으면 모델 검증 결과와 실제 적용 데이터의 분포 차이를 같이 판단할 수 있습니다.

### 운영 적용 시 유의사항
- 결론적으로 본 모델은 현재 데이터 범위 내 예측 지원에 사용할 수 있으나, 검증 세트는 30개 row 기준으로 평가되었습니다.
- 신규 설계 조건이나 기존 데이터 범위를 벗어난 입력에 대해서는 예측 신뢰도가 낮아질 수 있으므로 별도 검증이 필요합니다.

### 후속 작업
- 신규 시험 데이터 또는 별도 holdout set으로 재검증하고, 오차가 큰 row의 입력 변수 조건을 우선 점검해야 합니다.
- 후보 모델 간 성능 차이가 작다면 성능 지표뿐 아니라 해석성, 추론 속도, 재현성을 함께 기준으로 최종 운용 모델을 선택해야 합니다.